apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration
  namespace: easyshop-ns
spec:
  template:
    spec:
      containers: 
        - name: migration
          image: snehalpawar2945/easyshop-migration:3
          imagePullPolicy: Always
          env:
            - name: MONGODB_URI
              value: "mongodb://mongodb-svc:27017/easyshop"
      
      restartPolicy: OnFailure
